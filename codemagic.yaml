workflows:
  flutter_build:
    name: Flutter Build
    environment:
      flutter: latest
    scripts:
      - name: Build APK without sound null safety
        script: flutter build ipa --no-sound-null-safety